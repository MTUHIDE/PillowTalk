class TextParser:
    def __init__(self):

    def saveCommand(self, command):
        counter = 0
	try:
            f = open("commandsDB", 'r'):
	    for line in f:
		pass
	    if line != None:
		if word.isDigit(command.split()[0])
		    counter = int(word)
		    break
        except IOError:

	finally:
	    f.close()
